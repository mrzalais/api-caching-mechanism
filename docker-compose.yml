version: '3.8'

services:
  app:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: printful-catalog-api-cache-app
    restart: always
    working_dir: /var/www/
    volumes:
      - .:/var/www
