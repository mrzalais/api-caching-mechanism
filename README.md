# Printful API caching mechanism

## Installation
``docker compose up -d --build``

### Inside container

Install dependencies

``composer install``

Run tests

``./vendor/bin/phpunit``

Use the cache mechanism by running in root directory

``php index.php``